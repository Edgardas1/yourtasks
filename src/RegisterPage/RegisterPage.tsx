import { Link } from "react-router-dom";

const RegisterPage = () => {
  return (
    <div>
      <Link to="/" className="underline">
        Login
      </Link>
    </div>
  );
};

export default RegisterPage;
